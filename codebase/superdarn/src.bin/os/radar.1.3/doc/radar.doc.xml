<?xml version="1.0" encoding="ISO-8859-1"?>
<binary>
<project>superdarn</project>
<name>radar</name>
<location>src.bin/os/radar</location>

<syntax>radar --help</syntax>
<syntax>radar [<ar>options</ar>]</syntax>

<option><on>--help</on><od>print the help message and exit.</od>
</option>
<option><on>--version</on><od>print the RST version number and exit.</od>
</option>
<option><on>-vb</on><od>verbose. Log information to the console.</od>
</option>
<option><on>-lp <ar>lport</ar></on><od>listen for messages to the USRP ROS server on the local port number <ar>lport</ar> (default is 45000).</od>
</option>
<synopsis><p>A dummy radar task that simulates the response of the radar hardware. Note the ROS IP address may need to be changed to 127.0.0.1 (or localhost), either in the radar site configuration file or via the control program command line options, for this task to work.</p></synopsis>

<description><p>A dummy radar task that simulates the response of the radar hardware. Note the ROS IP address may need to be changed to 127.0.0.1 (or localhost), either in the radar site configuration file or via the control program command line options, for this task to work.</p>
</description>

<example>
<command>radar &amp</command>
<description>Start the radar simulator for control programs to attach to.</description>
</example>

</binary>
